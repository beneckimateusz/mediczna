# mediczna

```shell
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# put segmentation_*.{mhd,raw} and volume_*.{mhd,raw} files here
mkdir data

python src/visualization.py --help
```
